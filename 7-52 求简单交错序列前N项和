#include<stdio.h>
int main()
{
	int N;
	scanf("%d",&N);
	
	int cnt,i,flag=1;
	double sum;
	for(cnt=1; cnt<=N; cnt++){
	
		sum+=flag*1.0/(1+3*(cnt-1));
		flag=-flag;
	}
	printf("sum = %.3f",sum);
	return 0;
}
