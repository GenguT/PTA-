#include <stdio.h>

int main(){

    int n,sum=0,mul=1;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
    	
    	for(int j=1;j<=i;j++){
    		mul*=j;
    	//	printf("mul=%d\n",mul);
    	
		}
    	sum+=mul;
    	mul=1;//若没有，此时mul的会保持 ，所以还是函数好友 
	}
	printf("%d",sum);

    return 0;
}
